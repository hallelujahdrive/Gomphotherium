namespace Gomphoterium {
  private const string ENDPOINT_ACCOUNT_VERIFY_CREDENTIALS = "/api/v1/accounts/verify_credentials";
  private const string ENDPOINT_APPS = "/api/v1/apps";
  private const string ENDPOINT_OAUTH_TOKEN = "/oauth/token";
}
